import HeroSection from './components/HeroSection'

// Scope: Coming-Soon-Hero gemäß Figma LANDING-Frame (Node 1767:372).
// Header, Sections und Footer existieren im Repo, sind aber bewusst NICHT
// gemountet — wir bauen jetzt strictly nur den LANDING-Frame.
export default function App() {
  return <HeroSection />
}
